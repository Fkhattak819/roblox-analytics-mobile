# Analytics authorization integration

September 7 live diff update: read-only CDK comparison now succeeds and preserves all existing resource logical IDs, including worker/secret/mapping. Infrastructure recovery/IAM synthesis assertions pass. This resolves the resource-inventory comparison step, not deployment, effective IAM, grant provisioning or application/native acceptance. Requested the actual creator user ID/ownership type before preparing any real grant.

Latest checkpoint: CDK now includes the deployed analytics worker, retained analytics secret, queue mapping, concurrency and visibility settings from master. API and worker roles have explicit denials on writing/deleting ACCESS# grant records; grant reads and worker transactional ConditionCheckItem are scoped to that prefix. Worker writes remain tenant-prefixed. Connection status publication now checks membership in the same transaction as its write, and its restored API checks authorization before and after reading.

Worker tests exercise cross-account forged messages, revocation during credential lookup, revocation during sync, and both independent authorized users. Infrastructure tests verify two functions/two secrets, the queue mapping, and grant-write denials. These are local synthetic/synthesis checks, not proof of deployed permissions. Newer UI reconciliation, live deployment resource diff, operator grant provisioning, recovery, and native tests remain pending. Historical paragraphs below describe earlier integration stages.

September 7: recovered the newer backend analytics modules and contract from remote master 32ef869 while retaining the security branch's v2 login, session, limiter and logging changes. Frontend and infrastructure reconciliation remain unfinished. Do not deploy this partial integration.

Access now requires an operator-owned DynamoDB record with PK `ACCESS#<Roblox user ID>`, SK `UNIVERSE#<universe ID>`, type `analytics-access`, and enabled true. Missing/disabled/malformed records deny access. No mobile input grants membership, and no grant-creation API has been added. Provisioning must verify the creator's authorization for that universe before records are created; no real grants were created during this work.

Snapshot API requests derive ownerSub from the verified session. They check access before reading and again before returning. Sync jobs check before acquiring the queue gate and again before enqueue. The worker checks membership before reading the analytics credential. DynamoDB snapshot publication includes a transaction ConditionCheck of the grant, so revocation before the commit blocks the snapshot write.

Current regressions cover denied cross-universe API reads/queue requests, access removal, revocation while acquiring a gate, consistent membership reads, and the transaction shape/failure propagation. They use synthetic stores and command inspection. Live DynamoDB concurrency and full worker replay/revocation tests are still required. Grants removed and later re-added are currently treated as restored access; generation-bound queue grants remain a design consideration for stale jobs.

Next integration steps: preserve the deployed worker/secret/SQS mapping in CDK, add narrowly scoped ConditionCheckItem permissions for transactional grant checks, prevent runtime roles from creating grants, restore the connection-status API with access checks, and reconcile newer master screens under live Figma review. Extend worker tests and deployment diff verification before any cloud mutation.
